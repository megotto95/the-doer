function ToDo(name) {
    const title = name
    const desc = "Project Description Here"
    const dueDate = "01/01/2000"
    const priority = 1
    return {title, desc, dueDate, priority}
}

export default ToDo